<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:key="@string/order_by_key"
        android:title="@string/order_by_label"
        android:defaultValue="@string/order_by_default_value"
        android:entries="@array/order_by_list_titles"
        android:entryValues="@array/order_by_list_values"/>
</PreferenceScreen>
